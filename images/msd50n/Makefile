export PLATFORM = msd50n
export PRODUCT = msd50n

export BULID_TYPE = pkg

include ../common.mk

BR2_DL_DIR ?= ../../archive

all:
	cp latest/$(PRODUCT).tar.bz2 $(BR2_DL_DIR)/$(PRODUCT)-laird-$(LAIRD_RELEASE_STRING).tar.bz2
